 function padString(string, count, symbol, bool) {
    if (string === null) {
        return 'Данные по строке отсутствуют';
    } else if (count === null) {
        return 'Данные по количеству отсутствуют';
    } else if (symbol === null) {
        return 'Данные по символу отсутствуют';
    } else if (bool === null) {
        return 'Данные по расположению отсутствуют';
    }

    let strLength = string.length;
 }